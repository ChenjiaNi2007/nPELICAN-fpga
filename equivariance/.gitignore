# Large, regenerable intermediates (kept locally, not committed).
# Regenerate with: gen_boosted_inputs.py (canonical/*), run_sweep.py (logits),
# compute_metrics.py (results.csv).
canonical/equiv_pmu.dat
canonical/equiv_nobj.dat
canonical/equiv_beams_fixedbeams.dat
canonical/equiv_beams_boostedbeams.dat
canonical/manifest.csv
results/logits_*.dat
results/results.csv
results/results_*.csv
*.log

# Committed deliverables of a real run:
#   canonical/meta.json              (seed, directions, selected jets — reproducibility)
#   results/aggregates*.csv          (compact per-(config, beta) metrics; per-mode too)
#   results/plots*/*.png             (the curves, per-mode + boosted-vs-fixed overlay)
__pycache__/
*.tar.gz
.DS_Store
